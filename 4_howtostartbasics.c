#include <stdio.h>

int main()
{
    printf("hey what is this? \c what did you \c with?");

    /* to answer what \c does... it does nothing. I only see c character. could have been done without backslashes. I also got warning from compiler. */

    return 0;
}